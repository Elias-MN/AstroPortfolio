---
interface Props {
  title: string;
  image: string;
  description: string;
  hrefExecutable: string;
  hrefCode: string;
}

const { title, image, description, hrefExecutable, hrefCode } = Astro.props;
---

<div class="card glass w-64">
  <figure>
    <img src={image} alt="car!" />
  </figure>
  <div class="card-body">
    <h2 class="card-title">{title}</h2>
    <p>{description}</p>
    <div class="card-actions justify-end">
      <button class="btn btn-primary">
        <a href={hrefExecutable}>Ejecutable</a>
      </button>
      <button class="btn btn-primary">
        <a href={hrefCode}>Código</a>
      </button>
    </div>
  </div>
</div>
